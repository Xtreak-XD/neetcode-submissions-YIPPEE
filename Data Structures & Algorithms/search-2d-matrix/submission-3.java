class Solution {
    public boolean searchMatrix(int[][] matrix, int target) {
        int[] row = matrix[0];
        int high = matrix[0][row.length - 1];
        System.out.print(high);  

        return false; 
    }
}
